import DiscordClient from "@/components/DiscordClient";

export const metadata = {
  title: 'Clipify Post - Bot Message Sender',
};

export default function DiscordPage() {
  return <DiscordClient />;
}